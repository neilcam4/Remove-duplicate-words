function removeDuplicateWords(string){
    var newArray = string.split(' ').sort()
 
// if (string.length > 0) {
//     newArray.forEach(function(item){
//         newArray.push(item)
//     })
    return newArray
// }
}